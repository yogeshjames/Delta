 npm create vite@latest my-project -- --template react

npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

inside tailwind.config.js

/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

inside index.css

@tailwind base;
@tailwind components;
@tailwind utilities;
npm install
also give npm nstall inside the server folder


npm run dev
